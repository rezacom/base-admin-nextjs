const config = {
  plugins: ["@tailwindcss/postcss"],
  theme: {
    extend: {
      colors: {
        // primary
        primary: "red",

        black: "#000",
        white: "#fff",
      },
    },
  },
};

export default config;
